{
  "name": "meeting-assistant",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --parallel dev",
    "build": "pnpm --parallel build"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}